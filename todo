* Possibilité d'enlever une photo d'un cluster
* Mettre les photos retirés dans un cluster a part

Mettre un formulaire pour editer les photos du cluster
  * On peut supprimer la photo du cluster
  * On peut créer un nouveau cluster pour y mettre la photo
  * On peut changer la photo de cluster
  * Un cluster ne peut pas etre vide (pas de photo)


Mettre des tags pour chaque photos.
Avoir une fenetre lors de l'edition de la photo avec un input autocompletion

Faire du cluster un modele de données

On peut affiché les photos par tag

Rajouter une balise pour dire que l'on a deja essayé de trouve le nom de la place, la supprimer si on change une information